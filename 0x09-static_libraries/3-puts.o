#include "main.h"
/**
* _puts - program prints a string to stdout
* @s: the string to be printed
* _putchar ptints a new line
*/
void _puts(char *s)
{
	while (*s)
		_putchar(*s**);

	_putchar('\0');
}
