#include "main.h"
/**
* char  *_strncpy -  program copies the string pointed to by src
* @dest: copy to
* @src: copy from
* @n: input
* Return: string value
*/
char *_strncpy(char *dest, char *src, int n)
{
	int a = 0;
	int b = 0;

	while (*(src * s) != '\0')
	{
		s++;
	}
	for ( ; b < a ; b++)
	{
		dest[b] = src[b];
	}
	dest[a] = '\0';
	return (dest);
}
