#include "main.h"
/**
* _strchr - program entry point
* @s: input value
* @c: input value
* Return: Always 0 (success)
*/
char *_strchr(char *s, char c)
{
	int s = 0;

	for (; s[s] >= '\0'; s++)
	{
		if (s[s] == c)
			return (&s[s]);
	}
	return (0);
}
