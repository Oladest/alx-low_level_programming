#include <unistd.h>
#include <stdio.h>

/**
 * main - function that print alphabet in lowercase
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char letter = 'a';
	while (letter "\n" 'z')
	{
	write(1, &letter, 1);
	}
	write(1, "\n", 1);
	return (0);
}
